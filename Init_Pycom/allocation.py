import msgflow
import network
class Allocation:


    def __init__(self, flow, net, crit_level):
        self.msg_flow = flow
        self.network = net
        self.crit_level = crit_level

    def get_flow():
        return self.msg_flow

    def get_network():
        return self.network

    def get_crit_level():
        return self.crit_level

    def to_string():
        string = self.net.get_name() + "<-" + get_crit_level() + "-" + self.flow.get_name()


def main():

    


if __name__ == '__main__':
    main()
